# Improve Existing Custom HTML (Production Ready)

## Context

I have already added my custom HTML, CSS, and JavaScript into the
project.

### IMPORTANT

-   Do **NOT** redesign the website.
-   Do **NOT** replace my HTML.
-   Do **NOT** generate a new layout.
-   Do **NOT** change the existing structure or section order.
-   Work only on improving the existing implementation.
-   Preserve the current HTML structure and functionality.

------------------------------------------------------------------------

## Development Rules

-   Keep all existing HTML elements.
-   Do not rename classes or IDs unless absolutely required.
-   Reuse existing CSS whenever possible.
-   Only add minimal CSS and JavaScript needed.
-   Maintain responsive behavior.
-   Preserve animations unless fixing bugs.
-   Do not break any existing functionality.
-   Keep the code clean and production-ready.

------------------------------------------------------------------------

# Global Improvements

## Layout

-   Every section should fit within one viewport as much as possible.
-   Maintain consistent spacing between sections.
-   Keep equal left and right margins.
-   Ensure all content aligns consistently.
-   Maintain a proportional layout across all pages.

## Alignment

-   Left-aligned sections start at the same X position.
-   Right edges line up consistently.
-   Centered content is perfectly centered.
-   Cards have equal spacing.
-   Headings align consistently.

## Responsive Design

Optimize for:

-   Desktop
-   Laptop
-   Tablet
-   Mobile

Requirements:

-   No horizontal scrolling.
-   No overlapping elements.
-   No cropped content.

------------------------------------------------------------------------

# Light & Dark Mode

Audit every section.

Ensure:

-   Every text remains readable.
-   Icons remain visible.
-   Graphics maintain proper contrast.
-   Borders are visible.
-   Shadows remain balanced.
-   Glassmorphism works in both themes.
-   Hover states work in both themes.

Fix any invisible text or low-contrast elements.

------------------------------------------------------------------------

# Landing Page

Improve only the existing section.

-   Increase logo size.
-   Make transparent logos more visible.
-   Slightly increase the white logo strip height.
-   Increase partner logo sizes.
-   Maintain proportional spacing.

------------------------------------------------------------------------

# What is CASK Section

Improve the existing layout.

Requirements:

-   Video alignment should perfectly match the text block height.
-   Left and right columns should begin and end at the same positions.
-   Audio button should:
    -   Open muted.
    -   Play audio when clicked.
    -   Support pause/play.
    -   Have smooth transitions.

------------------------------------------------------------------------

# Four Years, Four Transformations

Fix:

-   Invisible text in Light Mode.
-   Decorative ring visibility.
-   Moving circle visibility.
-   Maintain identical appearance in Dark Mode.

------------------------------------------------------------------------

# Everything Inside CASK

Improve hover effects.

## Light Mode

-   Card becomes fully white.
-   Subtitle becomes readable.
-   Icon remains visible.
-   Title remains readable.

## Dark Mode

Prevent:

-   Subtitle overlapping.
-   Icon overlapping.
-   Heading overlapping.

Maintain proper spacing.

## Glow Effect

Currently only AI Tutor and Certification have glow.

Apply the same premium glow to every card.

Enhance with:

-   Outer glow.
-   Soft shadow.
-   Subtle elevation.
-   Smooth hover transition.

------------------------------------------------------------------------

# Multilingual Section

Improve layout.

Requirements:

-   Entire section fits within one viewport.
-   Language bubbles appear in one horizontal row.
-   Floating animation.
-   Interactive hover effect.
-   Hovering a language updates the sample statement dynamically.
-   Smooth transitions.

------------------------------------------------------------------------

# Four Years, Four Milestones

Maintain existing design.

Improve:

-   Glassmorphism consistency.
-   White Mode glass effect.
-   Badge contrast.
-   Proper spacing.
-   Entire section fits in one viewport.

------------------------------------------------------------------------

# The Phish & Chip Show

Improve the existing implementation only.

Requirements:

-   Fit every section within one screen.
-   Support complete Light Mode.
-   Maintain Dark Mode quality.
-   Fix spacing after the game ends.
-   Verdict section should move upward immediately without leaving empty
    space.
-   Chips Detective Lab should fit within the viewport.

------------------------------------------------------------------------

# Animation Improvements

Keep existing animations.

Improve only if necessary.

Requirements:

-   Smooth hover animations.
-   Smooth fade transitions.
-   Smooth scroll behavior.
-   Consistent animation timing.
-   No layout shift.
-   60fps animations.

------------------------------------------------------------------------

# Accessibility

Ensure:

-   Proper contrast ratios.
-   Keyboard navigation.
-   Visible focus states.
-   Accessible buttons.
-   Accessible interactive elements.

------------------------------------------------------------------------

# Performance

Do not introduce heavy libraries.

Optimize:

-   CSS
-   JavaScript
-   Animations
-   Repaints
-   Reflows

Keep bundle size minimal.

------------------------------------------------------------------------

# Final QA Checklist

Before completing, verify:

-   No broken layouts.
-   No overlapping text.
-   No invisible text.
-   No cropped content.
-   Consistent spacing.
-   Equal alignment.
-   Responsive across all screen sizes.
-   Fully functional Light Mode.
-   Fully functional Dark Mode.
-   Smooth hover effects.
-   Existing HTML preserved.
-   Existing functionality preserved.
-   Clean production-ready code.

------------------------------------------------------------------------

# Success Criteria

The final result should look like a polished production website while
preserving my custom HTML and overall design. The improvements should
focus on alignment, spacing, responsiveness, light/dark mode
consistency, hover effects, animations, and usability---without
redesigning the UI or changing the existing structure.
